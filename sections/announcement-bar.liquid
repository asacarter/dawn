{%- if section.settings.show_announcement_bar -%}
  {%- if section.blocks.size > 0 -%}
    <div class="announcement-bar{% if section.settings.show_line_separator %} announcement-bar-has-separator{% endif %}">
      <div class="page-width">
        <ul class="announcement-slider{% if section.settings.show_arrows %} announcement-slider-has-arrows{% endif %}{% if section.settings.announcement_compact %} announcement-slider-compact{% endif %}">
          {%- for block in section.blocks -%}
            <li class="announcement-bar__announcement" role="region" aria-label="{{ 'sections.header.announcement' | t }}" {{ block.shopify_attributes }}>
              {%- if block.settings.link != blank -%}
                <a href="{{ block.settings.link }}" class="announcement-bar__link">
                  {%- endif -%}
                  <span class="announcement-bar__heading">
                    {{ block.settings.heading | escape }}
                  </span>
                  {%- if block.settings.text != blank -%}
                  <span class="announcement-bar__text">
                    {{ block.settings.text | escape }}
                  </span>
                  {%- endif -%}
                  {%- if block.settings.link != blank -%}
                </a>
              {%- endif -%}
         	</li>
      	  {%- endfor -%}
        </ul>
      </div>
    </div>
  {%- endif -%}
{%- endif -%}

{% schema %}
{
  "name": "t:sections.announcement-bar.name",
  "settings": [
    {
      "type": "header",
      "content": "Announcement bar"
    },
    {
      "type": "checkbox",
      "id": "show_announcement_bar",
      "default": true,
      "label": "Show announcement bar"
    },
    {
      "type": "checkbox",
      "id": "announcement_compact",
      "label": "Use compact style"
    },
    {
      "type": "checkbox",
      "id": "show_arrows",
      "label": "Show navigation arrows"
    },
    {
      "type": "checkbox",
      "id": "show_line_separator",
      "default": true,
      "label": "t:sections.header.settings.show_line_separator.label"
    }
  ],
  "max_blocks": 12,
  "blocks": [
    {
      "type": "announcement",
      "name": "t:sections.announcement-bar.blocks.announcement.name",
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "default": "Next day delivery",
          "label": "Heading"
        },
        {
          "type": "text",
          "id": "text",
          "default": "When you order before 1pm",
          "label": "t:sections.announcement-bar.blocks.announcement.settings.text.label"
        },
        {
          "type": "url",
          "id": "link",
          "label": "t:sections.announcement-bar.blocks.announcement.settings.link.label"
        }
      ]
    }
  ],
  "default": {
    "blocks": [
      {
        "type": "announcement"
      }
    ]
  }
}
{% endschema %}
